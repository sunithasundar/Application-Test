export interface Product {
    id: number; //id accepts only number
    name: string; //name accepts only string
    state: string; //state accepts only string
    zip: number; //zip accepts only number
    amount: number; //amount accepts only number
    qty: number; //qty accepts only number
    item: string; //item accepts only string
};